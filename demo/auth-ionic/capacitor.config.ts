import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'sz.ionic.starter',
  appName: 'ionic-auth',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
}

export default config
